# Berry Medical Nuxt 项目规范

## 技术栈

| 分类 | 技术 |
|------|------|
| 框架 | Nuxt 3 + Vue 3 (Composition API) |
| 样式 | Tailwind CSS (brand 色系: orange/amber) |
| 类型 | TypeScript strict mode |
| 数据库 | Supabase (PostgreSQL) |
| 部署 | GitHub Pages (SSG) |
| 语言 | 中文 (zh-CN) |

## 目录结构

```
├── .github/workflows/
│   └── deploy.yml           # GitHub Actions 部署配置 ⭐
├── scripts/
│   └── fetch-supabase-data.ts  # 数据拉取脚本 ⭐
├── supabase/
│   ├── schema.sql           # 数据库表结构定义 ⭐
│   └── seed.sql             # 初始数据脚本 ⭐
├── components/              # Vue 组件 (PascalCase)
├── composables/             # 组合式函数 (useXxx.ts)
├── pages/                   # 页面 (自动路由)
├── layouts/                 # 布局
├── data/
│   └── generated/           # Supabase 自动生成 (勿手动编辑)
├── types/                   # TypeScript 类型
├── assets/css/              # 全局样式
├── public/images/           # 图片资源
└── nuxt.config.ts           # Nuxt 配置
```

## 核心文件说明

### 1. `scripts/fetch-supabase-data.ts`

数据拉取脚本，从 Supabase 获取数据并生成 JSON 文件。

**功能**：
- 并行获取 `websites`、`pages`、`blocks`、`news` 四张表数据
- 处理数据格式（字段名转换 snake_case → camelCase）
- 输出到 `data/generated/*.json`

**使用方式**：
```bash
pnpm fetch-data
# 或
node scripts/fetch-supabase-data.ts
```

**数据流向**：
```
Supabase → fetch-supabase-data.ts → data/generated/*.json → 组件使用
```

**输出文件**：
| 文件 | 说明 |
|------|------|
| `website.json` | 网站基础配置 |
| `pages.json` | 页面列表（用于导航） |
| `blocks.json` | 页面区块内容 |
| `news.json` | 新闻列表 |

### 2. `supabase/schema.sql`

数据库表结构定义，采用 **网站-页面-区块** 三层架构：

| 表名 | 说明 | 主键 |
|------|------|------|
| `websites` | 网站配置 | TEXT (id='main') |
| `pages` | 页面定义 | UUID |
| `blocks` | 页面区块 | UUID |
| `news` | 新闻文章 | UUID |

**表结构特点**：
- `websites` 表：JSONB 存储 contact、nav_config、footer_config、seo
- `pages` 表：支持导航排序、可见性控制
- `blocks` 表：JSONB 存储区块配置，支持多种类型
- `news` 表：支持分类、标签、浏览量、发布时间

**RLS 策略**：所有表开启 RLS，公开只读已发布内容

### 3. `supabase/seed.sql`

初始数据脚本，包含：
- 网站基础配置
- 页面定义（首页、新闻页）
- 各页面区块内容
- 示例新闻数据

### 4. `.github/workflows/deploy.yml`

GitHub Actions 部署配置，main 分支推送自动触发。

**运行环境**：
- Node: 20
- pnpm: 9
- OS: ubuntu-latest

**部署流程**：
```yaml
1. Checkout 代码
2. Setup pnpm (v9)
3. Setup Node (v20)
4. pnpm install
5. pnpm fetch-data        # 拉取 Supabase 数据
6. pnpm generate          # 构建静态站点
7. Create SPA fallback    # 复制 index.html → 404.html
8. Upload artifact
9. Deploy to GitHub Pages
```

**环境变量**：
| 变量 | 来源 | 说明 |
|------|------|------|
| `SUPABASE_URL` | GitHub Secrets | Supabase 项目地址 |
| `SUPABASE_SERVICE_ROLE_KEY` | GitHub Secrets | 服务端密钥 |
| `NUXT_PUBLIC_CDN_BASE_URL` | CI 硬编码 | CDN 地址 |

## 数据库表结构

### websites（网站表）
```sql
- id: TEXT (PK, default='main')
- name, domain, company_name, slogan, description, logo, url, icp
- contact: JSONB (phone, email, address)
- nav_config: JSONB (logo, menuItems, ctaButton)
- footer_config: JSONB (copyright, description, columns)
- seo: JSONB (title, description, keywords)
- created_at, updated_at: TIMESTAMP
```

### pages（页面表）
```sql
- id: UUID (PK)
- website_id: TEXT (FK)
- name: TEXT (页面名称)
- path: TEXT (路由路径)
- description: TEXT
- nav_sort_order: INTEGER (导航排序)
- is_nav_visible: BOOLEAN (导航可见)
- is_footer_visible: BOOLEAN (页脚可见)
- seo: JSONB
- created_at, updated_at: TIMESTAMP
```

### blocks（区块表）
```sql
- id: UUID (PK)
- page_id: UUID (FK)
- type: TEXT (区块类型: hero/about/services/service-detail/advantages/projects/news/contact/cta)
- title: TEXT
- sort_order: INTEGER
- config: JSONB (区块配置)
- is_published: BOOLEAN
- created_at, updated_at: TIMESTAMP
```

### news（新闻表）
```sql
- id: UUID (PK)
- title: TEXT
- excerpt: TEXT
- content: TEXT
- date: DATE
- category: TEXT
- image: TEXT
- views: INTEGER
- tags: TEXT[]
- is_published: BOOLEAN
- created_at, updated_at: TIMESTAMP
```

## 代码规范

### Vue 组件
```vue
<script setup lang="ts">
const props = defineProps<{ title: string }>()
</script>

<template>
  <!-- ========== Hero 首屏 ========== -->
  <section>...</section>
</template>
```

### Composables
- 文件名: `useXxx.ts`
- 返回命名对象，便于解构

### TypeScript
- 启用 strict mode
- 禁止隐式 any
- 优先使用 `const`

## 样式规范

- 品牌色: `brand-*` (50-900)
- 响应式: `sm md lg xl 2xl`
- Dark mode: `dark:` 前缀
- 图标: Font Awesome (`fas fab`)
- 图片: 使用 `useCdnUrl()` 处理 CDN 路径

## SEO 规范

- 使用 `useSeo()` composable
- 必须包含: title, description, og tags
- 语言: `lang="zh-CN"`

## 开发命令

```bash
pnpm dev          # 开发模式
pnpm fetch-data   # 拉取 Supabase 数据 (部署前必须执行)
pnpm generate     # 构建静态站点
pnpm preview      # 预览构建结果
```

## 部署

### 访问地址
- `https://wumacms.github.io/berry-medical-web/`
- baseURL: `/berry-medical-web/`

### 预渲染路由
| 路由 | 说明 |
|------|------|
| `/` | 首页 |
| `/news` | 新闻列表 |
| `/news/:id` | 新闻详情 |

### 部署前检查
- [ ] 运行 `pnpm fetch-data`
- [ ] 页面无 404
- [ ] 图片正常加载
- [ ] 响应式布局正常

## 禁止事项

- ❌ 手动编辑 `data/generated/` 目录
- ❌ 使用 `var` 声明变量
- ❌ 使用 Options API
- ❌ 硬编码敏感信息
- ❌ 模板内联样式 (除动态计算外)
